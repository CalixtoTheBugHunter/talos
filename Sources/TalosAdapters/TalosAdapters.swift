// The agent adapter layer — the only module permitted to spawn a subprocess.
//
// Deliberately empty: this target exists so the module graph in
// `ARCHITECTURE.md` builds, and each adapter is its own board item.
// https://github.com/CalixtoTheBugHunter/talos/wiki/Architecture-The-Orchestration-Boundary#agent-adapters
